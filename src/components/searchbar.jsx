import { styled } from "@mui/material";

export const SearchBar = styled("div")(({ theme }) => ({
  backgroundColor: "#fff",
  borderRadius: "15px",
  height: "2rem",
  flexGrow: "1",
  minWidth: "3rem",
}));
